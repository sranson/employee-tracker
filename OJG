


get the list of employees with an extra column for the manger name

select fname, lastname, title,  dept, salary, manager

select all employees where manager id is not null 

select A.Employees  AS  employees, B.employees AS managers 
from Employees A, Employees B
where manager id is not null 
